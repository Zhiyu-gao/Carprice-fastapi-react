server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # 关键：React Router 刷新不 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}
